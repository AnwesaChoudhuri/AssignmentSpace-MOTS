import configs.config_train_kittimots # use external given detections
import configs.config_test_kittimots  # use external given detections
import configs.config_test_kittimots_online  # use external given detections

import configs.config_train_kittimots_person # use external given detections
import configs.config_test_kittimots_person  # use external given detections


import configs.config_train_mot  # use external given detections (mot, not mots)
import configs.config_test_mot_FRCNN # use external given detections (mot, not mots)
import configs.config_test_mot_SDP # use external given detections (mot, not mots)
import configs.config_test_mot_DPM # use external given detections (mot, not mots)
import configs.config_test_mot_MOT17train # use external given detections (mot, not mots)
import configs.config_test_mot_2DMOT15train # use external given detections (mot, not mots)
import configs.config_train_kittimots_jt
import configs.config_test_kittimots_jt

import configs.config_test_motschallenge
